{
  "name": "paragon-part-match-deliverable",
  "private": true,
  "description": "Paragon take-home: catalog matcher UI + tests",
  "scripts": {
    "test": "node --test tests/matcher.test.js tests/build.test.js",
    "verify": "node verify.js",
    "build:data": "node build.js"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
